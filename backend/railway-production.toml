# Railway Production Configuration

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn app_production_v2:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHON_VERSION = "3.11"
